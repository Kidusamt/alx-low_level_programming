#include "main.h"
#include <stdio.h>
/**
 * main - prints the name
 * @argc: argument number
 * @argv: arry of string of argument
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i = argc;

	printf("%s\n", argv[i * 0]);
	return (0);
}
